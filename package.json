{
  "name": "gymbuilder",
  "private": true,
  "scripts": {
    "dev": "live-server --port=8080 --host=127.0.0.1 --no-browser --watch=."
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "three": "^0.162.0"
  }
}
